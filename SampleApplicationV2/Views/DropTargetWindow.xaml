<Window x:Class="SampleApplicationV2.Views.DropTargetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Drop target (another window)"
        Width="520" Height="420"
        Background="#0D0D1A"
        WindowStartupLocation="Manual">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Foreground="#9AA0B0" TextWrapping="Wrap" Margin="0,0,0,10"
                   Text="This is a SEPARATE WINDOW. Drag a row or a cell out of the grid and drop it on the panel below. It reads the payload the grid put on the DataObject."/>

        <!-- The actual drop target. AllowDrop + DragOver/Drop is all a receiver needs. -->
        <Border Grid.Row="1" x:Name="DropZone"
                AllowDrop="True"
                DragOver="DropZone_DragOver"
                DragLeave="DropZone_DragLeave"
                Drop="DropZone_Drop"
                Height="110"
                Background="#16162A" BorderBrush="#555577" BorderThickness="2"
                CornerRadius="4">
            <TextBlock x:Name="DropHint" Text="Drop rows here"
                       Foreground="#7F869A" FontSize="15"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <Border Grid.Row="2" Background="#101020" BorderBrush="#333355" BorderThickness="1"
                Margin="0,10,0,0" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ReceivedText" Foreground="#7FD1B9" FontFamily="Consolas"
                           FontSize="11" TextWrapping="Wrap"
                           Text="(nothing received yet)"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
