namespace SkiaSharpControlV2
{
    /// <summary>Aggregation type for grouped data fields.</summary>
    public enum SkAggregation
    {
        None,
        Sum,
        Avg,
        Min,
        Max,
        Count,
        Distinct
    }
}
